# Configuration de l'environnement (A copier vers .env)

# ==================== OIDC ====================
OKTA_DOMAIN=https://dev-xxxxxx.okta.com
OKTA_CLIENT_ID=votre_client_id_oidc
OKTA_CLIENT_SECRET=votre_client_secret_oidc
OKTA_AUTH_SERVER_ID=default
# La redirect URI est déterminée automatiquement, mais vous pouvez la forcer si besoin
# REDIRECT_URI=http://localhost:8000/oidc/callback

# ==================== SAML ====================
# Soit l'URL des metadatas, soit le chemin vers un fichier XML
OKTA_SAML_METADATA_XML=https://dev-xxxxxx.okta.com/app/xxxxxx/sso/saml/metadata
# OKTA_SAML_METADATA_XML=saml_metadata.xml

# ==================== SECRET ====================
# Clé secrète pour signer les sessions. Générez-en une aléatoire (ex: openssl rand -hex 32)
SECRET_KEY=une_cle_secrete_tres_longue_et_securisee_pour_les_sessions

# ==================== DEBUG / MOCK ====================
# Mettez à true pour utiliser de fausses données hors-ligne sans contacter Okta
MOCK_MODE=false
